# Set the the list of directories that cd searches.
CDPATH="$HOME/Projects"

# Define important directories
DESK="$HOME/Desktop"
DOWN="$HOME/Downloads"
PUB="$HOME/Public"

# List of directories that Node.js searcbes for modules.
export NODE_PATH="/usr/local/node/lib/node_modules"

# Location of the Node Version Manager
export NVM_DIR="/usr/local/nvm"

# Location of Perlbrew
export PERLBREW_ROOT="/usr/local/perlbrew"

# Location of rbenv
export RBENV_ROOT="/usr/local/rbenv"

# Location of pyenv
export PYENV_ROOT="/usr/local/pyenv"

# Network configuration
IP=$(ipconfig getifaddr en0)
# IP6=$(ifconfig en1 | grep 'inet6 ' | cut -d ' ' -f 2)
if [ ! $IP ]; then
	IP=$(ipconfig getifaddr en1)
	if [ ! $IP ]; then
		IP=$(ipconfig getifaddr en2)
	fi
fi

DEFAULT_USER=vbwx
